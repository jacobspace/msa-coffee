DROP TABLE IF EXISTS MEMBER;

CREATE TABLE MEMBER (
    `ID` NUMBER AUTO_INCREMENT NOT NULL,
    `NAME` VARCHAR NOT NULL,
    `PHONE_NUMBER` VARCHAR NOT NULL,
    PRIMARY KEY (`ID`)
);

ALTER TABLE MEMBER ADD CONSTRAINT MEMBER_UNIQUE UNIQUE (NAME, PHONE_NUMBER);